
export const useIsOwner = () => {

    // const {isLoggedIn} = useSelector((state) => state.user)
    // let isOwner = false

    // if(isLoggedIn){
    //     product.owner === isLoggedIn ? isOwner = true : isOwner
    // }

    return "HOLA GIL"
}

